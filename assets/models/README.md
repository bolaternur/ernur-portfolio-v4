# V5 model slots

Place the two real GLB files here after running `setup-assets.ps1`:

```text
assets/models/robot/DECODE Simple Bot.glb
assets/models/study/3209-0001-0007.glb
```

The website also checks a few root/fallback paths so localhost remains tolerant while assets are being installed.
