Place the authentic robot model here:

- `DECODE Simple Bot.glb`

`START_LOCALHOST.bat` runs `setup-assets.ps1`, which searches Downloads/Desktop and copies this file here automatically when it finds it.
